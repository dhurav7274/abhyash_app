/* eslint-disable prettier/prettier */
import {StyleSheet} from 'react-native';

export const ForgotPasswordCss = StyleSheet.create({
  mainConatiner: {
    backgroundColor: 'blue',
    height: '100%',
  },

  header: {
    height:250,
    alignItems:'center',
    justifyContent:'center'
  }
});
